# Web framework + ORM
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.30

# Production WSGI server (used by Render)
gunicorn==22.0.0

# PostgreSQL driver - required when DATABASE_URL points at Postgres
# (Neon / Supabase / Render Postgres). Safe to leave installed for SQLite dev.
psycopg2-binary==2.9.9
